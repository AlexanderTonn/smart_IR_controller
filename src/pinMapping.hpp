#ifndef PIN_MAPPING_HPP
#define PIN_MAPPING_HPP

#include <Arduino.h>


    constexpr static uint8_t IR_RECEIVER = 0; // Pin to receive IR signal
    constexpr static uint8_t IR_TRANSMITTER = 1; // Pin to transmit IR signal

#endif // PIN_MAPPING_HPP